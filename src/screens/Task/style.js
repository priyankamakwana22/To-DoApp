import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  container: {flex: 1, margin: 10, alignItems:'center'},
  input: {
    width: '100%',
    borderWidth: 1,
    borderColor: '#555555',
    borderRadius: 10,
    marginBottom: 20,
    backgroundColor: '#ffffff',
    textAlign: 'left',
    fontSize: 20,
    paddingHorizontal: 10,
  },
});

export default styles;
